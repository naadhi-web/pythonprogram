string=input("enter a string")
string=string.lower()
vowels="aeiou"
vowelfound=set()
for char in string:
    if char in vowels:
        vowelfound.add(char)
if vowelfound:
    print("vowels found are",vowelfound)
else:
    print("no vowels found")
